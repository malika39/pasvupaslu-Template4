<!-- HEADER -->
<header class="row">

    <div class="user-header {% if app.user %}user-connect{% endif %}">
        <div class="d-inline-block img-header">
            <img src="{{ asset('assets/images/illustration/logo-header.png') }}">
        </div>

        <div class="d-sm-inline-block position-absolute text-center block-responsive {% if app.user %}line-user{% endif %}">
            <h3 class="text-spacing header-h3 mb-0">pas vu, pas lu</h3>
            <em><span class="sub-title">Le marque-page des nouveaux talents !</span></em>
            <a class="btn-default mt-3" href="{{ path('identification') }}">
                <span>Inscription / Connexion</span>
            </a>
            {# Block à affichier pour les personnes connecter #}
            {#<div class="navbar box-user text-left">#}
                {#<div class="dropdown show">#}
                    {#<a class="droplists" href="#" role="button">#}
					    {#<span class="online">#}
					    		{#<img src="https://www.w3schools.com/w3images/avatar6.png">#}
					    {#</span>#}
                    {#</a>#}
                {#</div>#}

                {#<div class="notify">#}
                    {#<div class="notification mr-4"><i class="fas fa-bell"></i> <p>0</p></div>#}
                    {#<div class="message"><i class="fas fa-comments"></i> <p>0</p></div>#}
                {#</div>#}

            {#</div>#}
            {# FIN - du block#}

        </div>
        <div class="d-inline-block position-absolute img-header-2" style="right: -10rem">
            <img src="{{ asset('assets/images/illustration/header-books.png') }}">
        </div>

        {# Block à affichier pour les personnes connecter #}
        {#<div class="menu-user">#}
            {#<div class="dropdown-menu text-center">#}
                {#<a class="dropdown-item" href="#">Ajouter un livre</a>#}
                {#<a class="dropdown-item" href="#">Mon Profil</a>#}
                {#<a class="dropdown-item" href="#">Se déconnecter</a>#}
            {#</div>#}
        {#</div>#}
        {# FIN - du block #}

    </div>

    <!-- Bare de recherche -->
    <div class="col-12 col-sm col-md-6 p-0">
        <div class="recherche_p float-sm-right">

            <div class="position-absolute" style="top: -74px; right: 0;">
                <img src="{{ asset('assets/images/illustration/bare-search.png') }}">
            </div>

            <form action="" id="searchthis" method="GET">
                <input id="search" name="q" type="text" placeholder="Rechercher" autocomplete="off">
                <input id="search-btn" type="submit" value="Rechercher">
            </form>

        </div>

        <div class="search-sub-text float-right mt-3 text-right">
            <em>"Toute littérature est assaut contre la frontière."</em>
            <strong><span style="color:rgba(255,255,255,1);">Franz Kafka</span></strong>
        </div>

    </div>
    <!-- Fin de la bare de recherche -->
</header>
<!-- FIN HEADER -->