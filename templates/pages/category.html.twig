{% extends 'base.html.twig' %}

{% block stylesheets '<link rel="stylesheet" href="http://pasvupaslu.com/css/24.css">' %}

{#{% block title 'Categories' %}#}
{% block title %}
    {{ categories.categorie }}
{% endblock %}

{% block body %}


    {#<div class="container">#}
        {#<h3 class="text-center m5">{{ categorie  }}</h3>#}


        {#{% if livres is not empty %}#}


        {#<div class="row mt-5">#}


            {#{% for livre in livres %}#}
                {#<div class="col-md-2 mt-2">#}
                    {#<div class="card" style="width: 18rem;">#}
                        {#<a href="{{ path('show_livre',{'id': livre.id }) }}" class="">#}
                            {#<img alt="{{ livre.titre }}" src="{{ asset ('assets/images/livre/'~livre.photo) }}" class="card-img-top" />#}

                            {#<div class="card-body">#}
                                {#<h5 class="card-title">{{ livre.titre }}</h5>#}
                            {#</div>#}
                        {#</a>#}
                        {#<a href="{{ path('register_user_profil',{'id': livre.user.id }) }} " class="btn btn-primary">#}
                            {#{{ livre.user.nom }} {{livre.user.prenom }}#}
                        {#</a>#}
                    {#</div>#}
                {#</div>#}
            {#{% endfor %}#}
        {#</div>#}
        {#{% else %}#}

            {#<div class="alert alert-danger">#}
                {#<strong>#}
                    {#Aucune publication pour le moment#}
                {#</strong>#}
            {#</div>#}

        {#{% endif %}#}
    {#</div>#}
    <div class="content">

        <div class="illustration" style="background-image: url(http://pasvupaslu.com/gallery_gen/2c2bc19f27256551b4de4793cb7ba389.jpg);">
            <span class="illustration-title">{{ categorie  }}</span>
        </div>

        <div class="separator"></div>

        <div class="row mx-auto text-white category-title pt-2 pb-3" style="background-color: #a80d49;">
            <div class="col-md-10">
                Vous êtes l'auteur d'un roman, vous souhaitez donner de la visibilité à votre oeuvre, rendez-vous à la page<br> <a href="">inscription</a> et remplissez le formulaire. Un membre de notre équipe reviendra vers vous.
            </div>
            <a href="#!"><img src="{{ asset('assets/images/illustration/btn-register.jpg') }}"></a>
        </div>

        <div class="container pb-5 pt-5">
            <div class="row">
                {% if livres is not empty %}
                {% for livre in livres %}
                    <div class="col-md-2 mt-2 mb-3">

                        <div class="card col-12" style="height: 15rem;">
                            <a href="{{ path('show_livre',{'id': livre.id }) }}" class="">

                                <img alt="{{ livre.titre }}" src="{{ asset ('assets/images/livre/'~livre.photo) }}" class="card-img-top; height: 8rem;" />
                                <div class="card-body">
                                    <h5 class="card-title">{{ livre.titre }}</h5>
                                </div>
                            </a>
                            {#<a href="{{ path('show_livre',{'id': livre.id }) }}"><strong>Consulter un extrait</strong></a>#}


                        </div>
                        <a href="{{ path('profil_auter',{'id': livre.user.id }) }} " class="btn btn-primary col-12">
                            {{ livre.user.nom }} {{livre.user.prenom }}
                        </a>
                    </div>
                {#<div class="col-md-3">#}
                    {#<div class="card text-center mb-3" style="margin-top: 0!important">#}
                        {#<div class="card-body">#}

                            {#<a href="{{ path('show_livre',{'id': livre.id }) }}" class="d-block">#}
                                {#<img alt="{{ livre.titre }}" src="{{ asset ('assets/images/livre/'~livre.photo) }}" class="mb-2 mx-auto" style="max-width: 75%; max-height: 200px">#}
                            {#</a>#}

                            {#<h6><a href="{{ path('show_livre',{'id': livre.id }) }}" class="text-dark">{{ livre.titre }}</a></h6>#}

                            {#<div class="text-muted small mb-3">#}
                                {#Alyana a réveillé les géants qui parcouraient jadis les terres d’Arran...#}
                            {#</div>#}

                            {#<a href="{{ path('show_livre',{'id': livre.id }) }}"><strong>Consulter un extrait</strong></a>#}
                            {#<a href="{{ path('profil_auter',{'id': livre.user.id }) }} " class="btn btn-primary">#}
                                {#{{ livre.user.nom }} {{livre.user.prenom }}#}
                            {#</a>#}
                        {#</div>#}
                    {#</div>#}
                {#</div>#}
                {% endfor %}


                {% else %}

                <div class="alert alert-danger">
                    <strong>
                        Aucune publication pour le moment
                    </strong>
                </div>

                {% endif %}
            </div>
        </div>


     </div>


{% endblock %}

